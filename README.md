# ⏰ JavaScript Digital Clock

A clean, responsive digital clock built with HTML, CSS, and JavaScript.

## Features
- Displays current time in 12-hour format with AM/PM indicator
- Updates continuously in real-time
- Styled with CSS Flexbox and custom dynamic background

## Technologies Used
- HTML5
- CSS3
- JavaScript (DOM Manipulation, `setInterval`, `Date` object)
